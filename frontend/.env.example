# Only needed in production, where the frontend and backend are on different
# origins. In local dev, Vite's proxy (see vite.config.js) forwards /api to
# http://localhost:5000 automatically, so this can stay unset.
VITE_API_BASE_URL=
